<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="RegexEditText">
        <!-- 正则输入限制 -->
        <attr name="inputRegex" format="string" />
        <!-- 常用正则类型 -->
        <attr name="regexType">
            <!-- 手机号（只能以 1 开头） -->
            <enum name="mobile" value="0x01" />
            <!-- 中文（普通的中文字符） -->
            <enum name="chinese" value="0x02" />
            <!-- 英文（大写和小写的英文） -->
            <enum name="english" value="0x03" />
            <!-- 整数（非 0 开头的数字） -->
            <enum name="count" value="0x04" />
            <!-- 用户名（中文、英文、数字） -->
            <enum name="name" value="0x05" />
            <!-- 非空格字符 -->
            <enum name="nonnull" value="0x06" />
        </attr>
    </declare-styleable>
    <style name="EditTextStyle">
        <item name="android:textSize">15sp</item>
        <item name="android:textColorHint">@color/color_bcbcbc</item>
        <item name="android:textColor">@color/color_4c4c4c</item>
        <item name="android:paddingTop">15dp</item>
        <item name="android:paddingBottom">14dp</item>
        <item name="android:paddingStart">18dp</item>
        <item name="android:paddingEnd">18dp</item>
    </style>

    <style name="item">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textAppearance">?android:attr/textAppearanceMedium</item>
        <item name="android:singleLine">true</item>
    </style>
    <style name="SplashTheme" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item><!--目前这个属性未知-->

        <item name="android:windowIsTranslucent">false
        </item><!--沉浸透明状态栏  可选，窗口是否是半透明，设置状态栏的效果，这个值也会影响状态效果-->
        <item name="android:windowDisablePreview">false
        </item><!--冷启动是用到的属性，窗口不可见时的预览图，为true时点击app图标会慢一会-->
<!--        <item name="android:windowBackground">@drawable/layer_launch2</item>&lt;!&ndash;替换启动白屏的图片&ndash;&gt;-->
        <!--<item name="android:windowBackground">@mipmap/ic_splash_logo</item>--><!--替换启动白屏的图片-->

        <!-- 在5.0后，增加了一个windowDrawsSystemBarBackgrounds属性，用来标志此窗口是否负责绘制系统栏背景，
        我们把它设成false，这样当它绘制windowBackground的时候，就会在NavigationBar之上。-->
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>

        <item name="android:windowTranslucentStatus">true</item><!--这行代码使布局占据状态栏空间-->

    </style>

    <style name="driver_BottomNavigationMenu">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:drawablePadding">3dp</item>
        <item name="android:button">@null</item>
        <item name="android:gravity">center</item>

        <item name="android:textColor">@color/driver_menu_tab</item>
        <item name="android:textSize">23sp</item>
        <item name="android:paddingTop">7dp</item>
        <item name="android:paddingBottom">4dp</item>
        <item name="android:background">@drawable/driver_menu_bg</item>
    </style>

    <style name="FullScreenDialog" parent="Theme.AppCompat.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>